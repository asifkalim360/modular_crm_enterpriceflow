package com.crm.modules.user.entity;
// enum = Enumeration
// Enum ka use: Fixed values define karne ke liye hota hai.
public enum Role {

    ADMIN,
    MANAGER,
    USER
}

// WHY ENUM? :: “Role fixed values hote hain. Enum type safety provide karta hai aur invalid role insertion avoid karta hai.”
